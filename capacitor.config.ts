import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Virtual_Tour',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
